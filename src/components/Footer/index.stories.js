import React from "react";

import { Footer } from ".";

export default {
  title: "Components/Footer",
  component: Footer,
};

export const Default = (args) => {
  return <Footer />;
};

Default.args = {};
